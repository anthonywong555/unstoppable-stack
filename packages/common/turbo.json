{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "dev": {
      "dependsOn": ["build"],
      "outputs": ["dist/**"]
    },
    "build:code": {
      "dependsOn": ["build"],
      "outputs": ["dist/**"]
    }
  }
}