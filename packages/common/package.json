{
  "name": "@boilerplate/common",
  "version": "0.0.1",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./dist node_modules .turbo",
    "dev": "pnpm run build --watch",
    "build": "tsup index.ts tools/index.ts --format cjs,esm --dts --outDir dist",
    "build.watch": "pnpm run build --watch",
    "lint": "TIMING=1 eslint \"**/*.ts*\""
  },
  "devDependencies": {
    "@boilerplate/tsconfig": "workspace:*",
    "eslint": "catalog:",
    "tsup": "catalog:",
    "typescript": "catalog:"
  }
}
